# Port the server listens on (Render provides this automatically).
PORT=8080

# Optional: restrict allowed WebSocket origins (comma-separated). Empty = allow all.
CLIENT_ORIGIN=

# Runtime mode
NODE_ENV=development
